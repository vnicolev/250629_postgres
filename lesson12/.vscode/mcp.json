{
  "servers": {
    "render_postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://{$USER}:{$PASSWORD}@{$HOST}/{$DBNAME}?sslmode=require"
      ]
    }
  }
}